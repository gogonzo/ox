% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ie.R, R/ox.R
\name{ox}
\alias{ox}
\alias{xo}
\title{Short hand if-else}
\usage{
ox(.f, ..., .else)

xo(.f, ..., .else)

ox(.f, ..., .else)

xo(.f, ..., .else)
}
\arguments{
\item{.f}{(\code{function}) evaluated as \code{.f(...)}. Must return single logical value}

\item{...}{arguments passed to the \code{.f}. First element of \code{...} will be
considered as a positive-replacement (so called "then").}

\item{.else}{negative-replacement. NOTE that if \code{.else} is not specified
directly by naming argument then the last unnamed argument (from \code{...}) will
be considered as a replacement (it will not be used in the \code{.f}).}
}
\description{
Short hand if-else functions for simple values switching.

Short hand if-else functions for simple values switching.
}
\details{
\if{html}{\figure{uml.jpg}{options: width="75\%" alt="Figure: uml.jpg"}}
\if{latex}{\figure{uml.jpg}{options: width=7cm}}

#' \code{ox} evaluates function \code{.f} which returns a single logical value and and
depending on the result \code{ox} returns:
\itemize{
\item on \code{TRUE} (then) - \code{ox} returns \code{...[1]}. First element in \code{...} is considered
as a  positive-replacement.
\item on \code{FALSE} (else) - \code{ox} returns \code{.else} (negative-replacement).
}

To invert the switch one can use \code{xo} which is equivalent of
\code{ox(Negate(.f), ..., .else)}.

\if{html}{\figure{ie_uml.jpg}{options: width="75\%" alt="Figure: ie_uml.jpg"}}
\if{latex}{\figure{ie_uml.jpg}{options: width=7cm}}

#' \code{ox} evaluates function \code{.f} which returns a single logical value and and
depending on the result \code{ox} returns:
\itemize{
\item on \code{TRUE} (then) - \code{ox} returns \code{...[1]}. First element in \code{...} is considered
as a  positive-replacement.
\item on \code{FALSE} (else) - \code{ox} returns \code{.else} (negative-replacement).
}

To invert the switch one can use \code{xo} which is equivalent of
\code{ox(Negate(.f), ..., .else)}.
}
\examples{
# return don't switch if TRUE
ox(NULL, .f = is.null, 1)
ox("text", .f = is, "character", "not a character")

# return replacement `y` if is TRUE
xo(NULL, .f = is.null, 1)
xo("text", .f = is, "character", "not a character")
# return don't switch if TRUE
ox(NULL, .f = is.null, 1)
ox("text", .f = is, "character", "not a character")

# return replacement `y` if is TRUE
xo(NULL, .f = is.null, 1)
xo("text", .f = is, "character", "not a character")
}
